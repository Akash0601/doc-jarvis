spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/docjarvis
    username: postgres
    password: ${DB_PASSWORD:akash123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080